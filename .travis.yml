language: objective-c
osx_image: xcode7.2
before_install:
  - pip install codecov
script:
  - set -o pipefail
  - xcodebuild -version
  - xcodebuild -showsdks

  - xcodebuild -workspace SwiftDailyAPI.xcworkspace -scheme "SwiftDailyAPI-iOS" -destination "OS=9.2,name=iPhone 6s Plus" test | xcpretty -c
after_success:
  - codecov
